# 입고 자동화 ~ 상품 중복 제어 시나리오 테스트 (2026-09-26)

2026-09-25에 만든 입고 자동화(명세서 대조·"지금 할 일" 카드·결과 카드·스캔 종료)와 상품 중복 제어(소 유니크·손 등록 차단·세트 제거)를
**사용자가 실제로 밟는 순서**로 이어 붙여 검증한다. 기능별 세부 케이스는 기존 테스트가 이미 하고, 여기서는 그 사이를 잇는 시나리오와
기존 커버리지 지도를 함께 적는다.

실행: `npx vitest run --config vitest.integration.config.mts tests/integration/scenarios.itest.ts` (로컬 Docker Supabase 필요, 사장님 실제 프로젝트와 분리)

## 새 시나리오 (`tests/integration/scenarios.itest.ts`, 10건 — 전부 통과)

| # | 시나리오 | 확인하는 것 |
|---|---|---|
| SC-1 | 정상 하루: 명세서 2줄 → 박스 2개 → 카드에서 마감 | 명세서를 올려도 재고 0 / 상품을 안 골라도 명세서 줄이 상품을 정함 / 두 박스가 줄에 자동 연결 / 카드가 scan → close로 바뀜 / 마감은 사유 없이 되고 재고를 안 바꿈 |
| SC-2a | 결품: 3줄 중 2박스만 옴 → 스캔 종료 → 사유 마감 → 다시 열기 → 늦은 박스 | 카드 scan → scan-finished / 종료 표시는 재고 불변 / 사유 없이 마감 거부·사유 있으면 마감 / 다시 열면 늦은 박스가 안 온 줄에 이어짐 / 재고 합계 정확 |
| SC-2b | 잘못 찍은 박스 취소 | 취소하면 재고 0으로 원복, 그 줄이 "안 온" 것으로 돌아가 카드가 close → scan으로 되돌아감 |
| SC-3a | 상품 중복 제어 여정: 같은 소 부위·등급 박스 3개 스캔 → 손 등록 시도 → 수정으로 겹치기 시도 → DB 직접 삽입 | 상품 1개만 생성·재고 12 / 소·돼지·닭/오리 손 등록 거부, 가공육·양은 허용 / 부위 미지정 상품을 채워 기존 키와 겹치면 거부 / 앱 검사를 우회한 DB 삽입도 23505로 거부 |
| SC-3b | 다른 공급사는 같은 키의 소 상품 가능 | 유니크가 공급사별 |
| SC-4 | 주문 확정 → 고객이 받는 거래명세서 이력번호·출고 라벨 (세트 제거 마이그레이션 122 회귀) | 선입선출 배정 박스 2개의 이력번호가 고객에게 조회됨 / 라벨에 `is_bundle` 열 없음 / 재고 요약이 4개 칸 / 다른 공급사는 0건 |

### 폰 파일함 시나리오 (SC-5, 4건) — "이메일로 받은 명세서를 파일함에 저장했다가 화면에서 올린다"

| # | 시나리오 | 확인하는 것 |
|---|---|---|
| SC-5a | 한국 엑셀 기본 CSV(CP949, 이름 `명세서.CSV`, MIME 없음) → 읽기 → 저장 → 박스 스캔 | 한글이 안 깨지고 줄·부위가 읽힘 / 한글 파일명이어도 원본이 보관됨 / 부위 칸이 있으면 상품이 "안심"·"채끝"으로 자동 생성 |
| SC-5b | 텍스트 PDF(이메일 첨부) / 깨진 파일 / 8MB 초과 / 파일 없음 | PDF는 표가 복원됨 / 나머지는 예외 없이 안내 문구로 거부되어 "원본만 보관"으로 넘어감 |
| SC-5c | 직원·고객·비로그인 | 직원은 됨, 고객·비로그인은 거부 |
| SC-5d | 사진(한글 파일명 `거래명세서 사진 (1).JPG`) | 품목 줄 없이 원본만 저장되고 보관됨 |

이 시나리오로 **실제 버그 2개를 찾아 고쳤다**(아래 "발견" 참고). 단위 테스트 `document-file-encoding.test.ts`(인코딩), `pdf-extract-email-statement.test.ts`(실제 PDF를 만들어 읽기), `document-storage-name.test.ts`도 추가.

## 기존 커버리지 지도 (어제 기능 → 이미 있는 테스트)

| 기능 (마이그레이션·커밋) | 테스트 |
|---|---|
| 명세서 번호 표기 경우의 수 — 하이픈·0 탈락·과학표기·LOT 헤더·번호 여럿·부속 줄·두 칸 서식 (115) | `lib/livestock/document-parser.test.ts` 25건 |
| 로트 ↔ 개체 다리, 스캔 먼저·명세서 나중 거슬러 확정, GTIN ↔ 명세서 충돌 (116·117) | `inbound.itest.ts` "relink_pending_scans_to_documents", "명세서 줄 ↔ 박스 연결"; `inbound-scan-result.test.ts` |
| 줄 상태·자동 배정·중복 창 우회·마감/다시 열기 (118), 취소 박스 제외 (119) | `inbound.itest.ts` "명세서 줄 ↔ 박스 연결", `documents.itest.ts` 13건, `document-reconciliation.test.ts` 15건 |
| 현장 "스캔 종료" 표시 (120) | `documents.itest.ts` "setDocumentsScanFinishedAction" 5건 |
| "지금 할 일" 카드 판단(문서 선택 포함) | `inbound-next-step.test.ts` 14건 + SC-1/2의 실제 DB 값 기반 검증 |
| 등록 결과 색 카드 | `inbound-scan-result.test.ts` |
| 소 자동 생성 정체성(부위·등급·원산지·명세서 등급 채움), 돼지·닭·오리 출처 키 자동 생성·동시 생성 | `inbound.itest.ts` "소 상품 자동 생성", "소 외 축종 자동 생성" |
| 소 DB 유니크·세트 예외 제거(121·122) | `scripts/db-test-cattle-identity-unique.sql` 5건 + `scripts/db-test-cattle-identity-concurrency.sh`(세션 2개 동시 생성, 패치 전 함수는 실패) |
| 이력 대상 축종 손 등록 차단 | `products.itest.ts` "이력 대상 축종…", `traceable-categories.test.ts`, SC-3a |
| 세트 제거 후 명세서·라벨·재고 요약 | SC-4, `db-test-stock-ledger.sql`, `db-test-product-management.sql`(44건), `db-test-tenant-gate-null.sql` |

## 이번에 확인·발견한 것

- 시나리오 테스트가 실제로 문제를 잡는지 **변조 검사**를 했다: 소 유니크 인덱스를 지우고 돌리면 SC-3a가 실패한다(인덱스 복원 확인).
- 동시성 스크립트는 패치 전 자동 생성 함수로 되돌리면 `PRODUCT_NOT_FOUND`로 실패하고, 패치 후 통과한다.
- **발견·수정 1 — 한국 엑셀 CSV(CP949) 글자 깨짐:** 명세서 CSV를 브라우저가 UTF-8로만 읽어, 한국 엑셀의 "CSV(쉼표로 분리)"(CP949)는 품목명이 깨졌다(깨진 부위로 이상한 상품이 자동 생성될 수 있음). `decodeDocumentFileText`가 UTF-8로 안 읽히면 EUC-KR로 다시 읽도록 고침(명세서 올리기·엑셀 대량 입고 둘 다).
- **발견·수정 2 — 한글 파일명이면 원본이 보관되지 않음:** Storage 객체 키가 ASCII만 받아 `거래명세서.pdf` 같은 이름은 InvalidKey(400)로 거부됐다. 사진·스캔본은 원본이 유일한 내용이라 치명적. 키를 `고유값.확장자`로 바꾸고 원래 이름은 `file_name`에 유지.
- **현재 설계로 남은 한계 (버그 아님, 결정 필요):** ① `.xlsx`는 읽지 않고 안내만 한다(폰 파일 선택창에도 안 뜸) — 이메일 첨부가 xlsx면 CSV 저장·복사 붙여넣기가 필요. ② 부위는 "부위" 칸이 있을 때만 읽는다 — 품목명에 섞여 오는 명세서(부위 칸 없음)는 상품이 "(부위 미지정)"으로 만들어진다. ③ 종이(실물) 명세서는 사진으로 보관만 되고 대조·자동 연결은 안 된다(글자 인식 없음).
- 발견한 버그: 동시성 스크립트가 로컬 DB의 `upsert_master_livestock` 권한을 `authenticated`에 열어 둔 채 끝나 다른 DB 테스트 3건이 실패했다 → 스크립트가 권한을 되돌리도록 수정(라이브 무관).
- 세트 번호(`SET-…`)를 입고 화면에서 거부하던 가드를 지웠다. 이제 그런 값은 형식만 맞으면 다른 이력번호처럼 조회 실패(예외) 스캔으로 기록된다(더는 발행하지 않는 번호라 의도된 결과).

## 이 테스트로 검증하지 못한 것 (실계정 클릭 검증 필요)

- 화면(React) 자체: 카드 문구·배지·색·탭 이동·자동 새로고침. 카드 **판단 로직**만 DB 값으로 검증했다.
- 명세서 파일 업로드(PDF/엑셀 파싱)와 정부 이력조회 API 실호출 — 인증키가 없거나 목킹이다.
- 마이그레이션 122는 라이브에 적용 완료(2026-09-26). 이 문서의 시나리오는 로컬 DB에서만 돌렸다.
